{
  "name": "lembic",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "frontend": "npm run dev --workspace=frontend",
    "backend": "cd backend && python -m lembic open --no-browser"
  }
}
